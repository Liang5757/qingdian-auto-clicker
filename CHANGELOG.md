# 更新日志

版本遵循语义化版本约定；用户可见变更在发布前记录。

## [1.2.0] - 2026-09-22

- 新增 macOS 13+ 原生 SwiftUI 版本，提供 Intel / Apple Silicon 通用应用包。
- 两端保持点击配置、F9 / F10、Esc 备用停止、后台驻留和可选登录启动功能。
- Mac 版增加辅助功能授权引导，缺少权限或 F10 不可用时禁止启动；旧会话回调无法继续发送。
- 统一 Windows / macOS CI 和发布流程，两个平台构建通过后才发布。
- Mac 包仅 ad-hoc 签名，未完成 Developer ID 签名、公证和真实桌面输入验收。

## [1.1.0] - 2026-09-22

- 按现代浅色设计稿重做界面：状态卡片、两列设置、分段式按键选择和蓝色开始按钮。
- 关闭或最小化窗口改为隐藏到系统托盘；全局快捷键和当前会话继续工作。
- 托盘菜单支持恢复、开始、停止、退出，运行与停止使用不同颜色图标。
- 增加启动时隐藏和当前用户开机启动选项，默认关闭；不会自动开始连点。
- 设置修改后自动保存；退出和 Windows 关机仍停止输出并清理托盘资源。
- 37 项逻辑测试及替代发送器下的托盘生命周期检查通过；不代表真实键盘端到端验证。

## [1.0.3] - 2026-09-21

- 统一输入输出入口，停止与发送串行化，阻止停止后的晚到 tick 继续发送。
- 独立消息线程监听 F10 / Esc，保留按键采样备用路径。
- 部分发送失败时仅补发必要的松开事件，避免零发送时释放物理按键。
- 增加随机输入标记、只读输入观测和有大小上限的本地诊断日志。
- 30 项测试在 x86 / x64 通过；持续右键反馈的完整触发链仍未确认，真实桌面端到端验收待完成。

## [1.0.2] - 2026-09-21

- 移除固定坐标和延时取点选项，始终点击鼠标当前位置。
- 删除 SetCursorPos 调用，点击事件不含鼠标移动标志。
- 兼容旧配置，忽略并在保存时移除旧坐标字段，保留其他设置。

## [1.0.1] - 2026-09-21

- 针对 F10 无法停止的反馈，增加独立线程按键状态采样与停止标记，作为全局热键消息之外的备用路径。
- 每轮点击前、启动延迟和取点期间检查 F10；按键松开后保留已捕获的停止请求。
- 标题显示版本，状态栏区分热键消息停止与按键检测停止。
- 此改进尚未在报告故障的真实桌面验证，不能视为该环境的根因已确认。

## [1.0.0] - 2026-09-21

### 新增

- 中文 Windows Forms 界面与全局 F9/F10 快捷键。
- 点击间隔、按键、单击/双击、定次与无限模式。
- 固定位置、鼠标跟随、延时取点、自动保存设置。
- 标准解决方案、xUnit 回归测试、锁定依赖与 Windows CI。
- 便携 ZIP 打包、SHA-256 校验及 GitHub 标签发布流程。

### 稳定性

- F10 占用时禁止启动，输入失败或无效坐标时停止。
- 配置原子替换、损坏配置回退及 XML 外部实体防护。
- 修复配置保存失败提示被覆盖的问题。

### 已知限制

- 尚未完成全套真实桌面、混合 DPI 与权限组合验收，详见 `docs/TESTING.md`。

# 安全政策

## 支持范围

当前维护最新 `1.x` 版本；旧版本问题优先通过升级解决。本项目由社区维护，不承诺固定响应时间。

## 报告漏洞

请通过仓库 [私密漏洞报告](https://github.com/Liang5757/qingdian-auto-clicker/security/advisories/new) 提交可复现步骤、受影响版本、预期影响和最小示例。不要在公开 Issue 中披露未修复漏洞、凭证或个人信息。

若该入口暂不可用，请通过维护者个人页的可用联系方式请求私密报告渠道；不要先公开漏洞细节。

## 安全边界

程序默认以当前用户权限运行，不自动提权、不联网、不收集遥测、不安装服务。配置保存在当前用户的本地应用数据目录。XML 读取禁止 DTD/外部实体，并限制文档大小。

F10 注册失败时禁止连点；输入失败时停止。应用无法保证模拟输入被目标窗口接受，也无法判断鼠标当前指向的操作是否敏感。不要将连点目标设置为购买、删除或授权确认等不可逆操作。

发行包尚未进行代码签名，可用随附 SHA-256 文件校验下载完整性。哈希不是代码签名，不能独立证明发布者身份。
